apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-java
spec:
  replicas: 3
  selector:
    matchLabels:
      app: web-java
  template:
    metadata:
      labels:
        app: web-java
    spec:
      containers:
      - name: java
        image: maho/web-java
